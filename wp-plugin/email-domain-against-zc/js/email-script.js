/*
document.querySelectorAll('input[name="email"]').forEach(function (input) {
        input.addEventListener("keypress", function (event) {
            if (event.key === ".") {
                event.preventDefault();
                alert("Dots are not allowed in this field!");
            }
        });
    });
*/